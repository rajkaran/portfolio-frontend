import { Container } from '@mui/material';

export default function Dashboard() {
  return (
    <Container>
      <div style={{ padding: '20px' }}>this is my dashbord for portfolio</div>

      {/* 3 filters - market[india, canada, usa] - class[dividend, trade] - sortby[category, most traded, gainers, close to threshholds]
      category - core, under watch, once in a while, avoid
      */}
    </Container>
  );
}
